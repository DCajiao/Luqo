apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: cluster-luqo
  region: us-east-1

managedNodeGroups:
  - name: ng-2
    instanceType: t3.large
    desiredCapacity: 3
    minSize: 2
    maxSize: 4
    volumeSize: 20
    iam:
      instanceRoleARN: arn:aws:iam::381491960576:role/LabRole